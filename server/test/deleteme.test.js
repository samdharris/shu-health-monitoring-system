describe('This is a demo to prove Sam can walk the walk.', () => {
  it('should add 5+5 to make 10', () => {
    // Arrange
    const expected = 10;

    // Act
    const actual = 5 + 6;

    // Expect
    expect(actual).toBe(expected);
  });
});
